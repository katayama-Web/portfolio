<h4>ジャンル追加</h4>
<%= form_with model: @genre, url: admin_genres_path do |f| %>
<%= f.text_field :name %>
<%= f.submit '追加する' %>
<% end %>
<h4>ジャンル一覧</h4>
<% @genres.each do |genre| %>
<%= genre.name %>
<% end %>